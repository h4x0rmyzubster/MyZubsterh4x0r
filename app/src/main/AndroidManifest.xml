<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permessi di rete -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Permessi per notifiche -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Permessi per fotocamera (QR Scanner) -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Permessi per archiviazione (condivisione QR) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyZubster"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="33">

        <!-- === MAIN ACTIVITY === -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- === PROFILE ACTIVITY === -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false" />

        <!-- === QR CODE ACTIVITY === -->
        <activity
            android:name=".ui.qr.QRCodeActivity"
            android:exported="false" />

        <!-- === PAYMENT STATUS ACTIVITY === -->
        <activity
            android:name=".ui.payment.PaymentStatusActivity"
            android:exported="false" />

        <!-- === FILE PROVIDER PER CONDIVIDERE QR === -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>